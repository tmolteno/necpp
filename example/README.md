#Using nec2++ from C/C++ code

This directory contains an example showing how to call nec2++ from within C or C++ to simulate
the performance of a wire structure.

###Author

Tim Molteno <tim@physics.otago.ac.nz>

##C example

    gcc -o test_nec test_nec.c -lnecpp
    ./test_nec

##C++ example

This example code does not work at the moment (The C++ API is not working at the moment). However the C example above can also be used within C code.
